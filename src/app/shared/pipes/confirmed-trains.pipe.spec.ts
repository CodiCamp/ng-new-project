import { ConfirmedTrainsPipe } from './confirmed-trains.pipe';

describe('ConfirmedTrainsPipe', () => {
  it('create an instance', () => {
    const pipe = new ConfirmedTrainsPipe();
    expect(pipe).toBeTruthy();
  });
});
